CREATE TABLE Token(
    TokenId INT GENERATED BY DEFAULT AS A PRIMARY KEY,
    TokenNo VARCHAR(20) UNIQUE,
    VisitNo TEXT NOT NULL,
    CreatedAt DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP 

);
CONSTRAINT VisitID 
            FOREIGN KEY (VisitId) 
            REFERENCES Visits(VisitId),